diff --git a/VintagestoryApi/Client/MeshPool/MeshDataPool.cs b/VintagestoryApi/Client/MeshPool/MeshDataPool.cs
index f9128b4..78c63aa 100644
--- a/VintagestoryApi/Client/MeshPool/MeshDataPool.cs
+++ b/VintagestoryApi/Client/MeshPool/MeshDataPool.cs
@@ -363,30 +363,46 @@ namespace Vintagestory.API.Client
         /// <param name="frustumCuller">The area where models can be viewed from the camera.</param>
         /// <param name="frustumCullMode">The mode of the culling.</param>
         public void FrustumCull(FrustumCulling frustumCuller, EnumFrustumCullMode frustumCullMode)
         {
             indicesGroupsCount = 0;
-            RenderedTriangles = 0;
-            AllocatedTris = 0;
 
-            for (int i = 0; i < poolLocations.Count; i++)
+            // Optimum: inline visibility checks and sum indices before one division.
+            // This keeps CullNormal's FrustumVisible side effect.
+            int visibleBufIndex = ModelDataPoolLocation.VisibleBufIndex;
+            int renderedIndices = 0;
+            int allocatedIndices = 0;
+            int count = poolLocations.Count;
+
+            for (int i = 0; i < count; i++)
             {
                 ModelDataPoolLocation location = poolLocations[i];
-
                 int size = location.IndicesEnd - location.IndicesStart;
-                if (location.IsVisible(frustumCullMode, frustumCuller))
+
+                bool visible = frustumCullMode switch
+                {
+                    EnumFrustumCullMode.CullInstant => !location.Hide && location.CullVisible[visibleBufIndex] && frustumCuller.InFrustum(location.FrustumCullSphere),
+                    EnumFrustumCullMode.CullInstantShadowPassNear => !location.Hide && location.CullVisible[visibleBufIndex] && frustumCuller.InFrustumShadowPass(location.FrustumCullSphere),
+                    EnumFrustumCullMode.CullInstantShadowPassFar => !location.Hide && location.CullVisible[visibleBufIndex] && frustumCuller.InFrustumShadowPass(location.FrustumCullSphere) && location.LodLevel >= 1,
+                    EnumFrustumCullMode.CullNormal => !location.Hide && location.CullVisible[visibleBufIndex] && (location.FrustumVisible = frustumCuller.InFrustumAndRange(location.FrustumCullSphere, location.FrustumVisible, location.LodLevel, location.OptimumUseChiselLodDistance)),
+                    _ => !location.Hide
+                };
+
+                if (visible)
                 {
                     indicesStartsByte[indicesGroupsCount * 2] = location.IndicesStart * 4; // Offset in bytes, not ints
                     indicesSizes[indicesGroupsCount] = size;
-
-                    RenderedTriangles += size / 3;
-
+                    renderedIndices += size;
                     indicesGroupsCount++;
                 }
 
-                AllocatedTris += size / 3;
+                allocatedIndices += size;
             }
+
+            // The renderer uses triangle lists, so one division yields the same stats.
+            RenderedTriangles = renderedIndices / 3;
+            AllocatedTris = allocatedIndices / 3;
         }
 
         public void SetFullyVisible()
         {
             indicesGroupsCount = 0;
@@ -513,10 +529,12 @@ namespace Vintagestory.API.Client
 
         public Bools CullVisible = new Bools(true, true);
 
         public int LodLevel = 0;
 
+        public bool OptimumUseChiselLodDistance;
+
         public bool Hide;
 
         /// <summary>
         /// Used for models with movements (like a door).
         /// </summary>
@@ -541,11 +559,11 @@ namespace Vintagestory.API.Client
 
                 case EnumFrustumCullMode.CullInstantShadowPassFar:
                     return !Hide && CullVisible[VisibleBufIndex] && culler.InFrustumShadowPass(FrustumCullSphere) && LodLevel >= 1;
 
                 case EnumFrustumCullMode.CullNormal:
-                    return !Hide && CullVisible[VisibleBufIndex] && UpdateVisibleFlag(culler.InFrustumAndRange(FrustumCullSphere, FrustumVisible, LodLevel));
+                    return !Hide && CullVisible[VisibleBufIndex] && UpdateVisibleFlag(culler.InFrustumAndRange(FrustumCullSphere, FrustumVisible, LodLevel, OptimumUseChiselLodDistance));
 
                 default:
                     return !Hide;
             }
 
